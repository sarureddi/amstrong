# amstrong
#include<stdio.h>
#include<conio.h>
int ams
{
int a,b,s,temp,digit,sum,n;
printf("enter the first value");
printf("enter the second value");
 for n in range(a,b)
   sum = 0;
   temp = s;
   while temp > 0
       digit = temp % 10;
       sum =sum+ digit ** 3;
       temp /= 10;
 
   if s == sum
   {
       printf("the interval value is",s);
   }
 }
   return 0;
     
